# ⚠️  IMPORTANT: This file is for REFERENCE ONLY
#
# **DO NOT CREATE .env.local** - All environment variables are managed via Netlify
#
# GEMINI_API_KEY Storage (Server-Side Only):
# - The Gemini API key is NEVER used in client-side code
# - It is stored exclusively in Netlify Site Settings → Build & Deploy → Environment
# - The frontend calls /.netlify/functions/gemini-proxy (Netlify Function)
# - The function securely accesses the key via process.env.GEMINI_API_KEY
#
# Local Development:
# - The app uses VITE_USE_MOCK_DATA=true (hardcoded default)
# - No API keys are needed locally
# - Mock data simulates backend responses with artificial delays
#
# Production (Netlify):
# - Set GEMINI_API_KEY in Site Settings → Build & Deploy → Environment
# - Set VITE_USE_MOCK_DATA=false to use real backend
#
# This approach ensures secrets are never exposed in the repository or client bundles

# PHASE 1-2: Backend Service Configuration
# Replace these with your actual backend URLs

# Postgres Database (via Supabase, Neon, or managed service)
VITE_API_BASE_URL=http://localhost:3001/api

# S3-compatible object storage (AWS S3, Wasabi, Cloudflare R2, etc.)
VITE_STORAGE_BUCKET=https://s3.example.com/managed-capture

# Auth provider configuration (OIDC-compatible)
# For MVP, this can be empty and use JWT from API
VITE_AUTH_PROVIDER_URL=https://auth.example.com

# Feature flags for transitioning from mock to real backend
VITE_USE_MOCK_DATA=true

# ============================================================================
# STAGE 1 DEPLOYMENT: Supabase Configuration
# ============================================================================
#
# IMPORTANT: These variables are configured in Netlify dashboard, NOT .env.local
# See docs/DEPLOYMENT_STAGE1.md for production setup instructions
#

# Supabase Project URL
# Get from: https://app.supabase.com/project/[YOUR_PROJECT_ID]/settings/api
# Example: https://abc123.supabase.co
VITE_SUPABASE_URL=https://your-project.supabase.co

# Supabase Anonymous Key (public, safe to expose to frontend)
# Get from: https://app.supabase.com/project/[YOUR_PROJECT_ID]/settings/api
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Storage Provider (default: supabase, future: storj)
VITE_STORAGE_PROVIDER=supabase

# ============================================================================
# LEGACY (Deprecated, kept for reference)
# ============================================================================

# JWT Configuration (if not using Supabase Auth)
# Secret key for signing JWTs (server-side only, never expose)
# VITE_JWT_SECRET=your-jwt-secret-here

# JWT expiration time in seconds (default: 3600 = 1 hour)
# VITE_JWT_EXPIRES_IN=3600

# Refresh token expiration in seconds (default: 604800 = 7 days)
# VITE_JWT_REFRESH_EXPIRES_IN=604800
